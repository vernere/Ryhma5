{
  "name": "app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev:backend": "bun run --cwd backend src/index.js",
    "dev:frontend": "bun run --cwd frontend dev",
    "start:backend": "bun run --cwd backend src/index.js",
    "start:frontend": "bun run --cwd frontend build && bun run --cwd frontend preview"
  }
}